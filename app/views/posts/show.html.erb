<h1><%= @post.title %></h1>
<p><%= @post.text %></p>
<%= link_to "Edit", edit_post_path(@post) %>|<%= link_to "Show all", posts_path %>|<%= link_to "Destroy", post_path(@post), data: {
  turbo_method: :delete,
  turbo_confirm: "Are you sure?"
} %>

<h2>Comments</h2>
<%= render @post.comments %>

<h2>Add a comment:</h2>
<%= render 'comments/form' %>
