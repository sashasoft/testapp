Our blog has <%= Post.public_count %> posts and counting!
<% @posts.each do |post|%>
  <% unless post.archived? %>
    <h3><%= post.title %></h3>
    <%= link_to "Schow", post_path(post) %>|<%= link_to "New", new_post_path %>|<%= link_to "Edit", edit_post_path(post) %>
  <% end %>
<% end %>